@using Microsoft.AspNetCore.Identity
@using WebApp.Models

@inject SignInManager<Uye> SignInManager
@inject UserManager<Uye> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        //Admin veya Uye olmasına göre özelleştir...

        if (@UserManager.IsInRoleAsync(UserManager.GetUserAsync(User).Result, "Admin").Result)
        {
            <li class="nav-item" style="width:180px">
                <a asp-action="Index" asp-controller="Admin" asp-area="AdminPanel">Admin Paneline Git</a>
            </li>
        }
        else
        {
            <li class="nav-item" style="width:180px">
                <a asp-action="Index" asp-controller="Uye" asp-area="UyePanel"> Uye Paneline Git</a>
            </li>
        }


        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
        </li>
        <li class="nav-item">
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>

<link rel="stylesheet" href="~/evonyee/css/bootstrap.min.css" />
<!-- site css -->
<link rel="stylesheet" href="~/evonyee/css/style.css" />
<!-- responsive css -->
<link rel="stylesheet" href="~/evonyee/css/responsive.css" />
<!-- colors css -->
<link rel="stylesheet" href="~/evonyee/css/colors.css" />
<!-- custom css -->
<link rel="stylesheet" href="~/evonyee/css/custom.css" />
<!-- wow animation css -->
<link rel="stylesheet" href="~/evonyee/css/animate.css" />
<!-- Revolution Loaling Fonts and Icons  -->
<link rel="stylesheet" href="~/evonyee/js/revolution/fonts/pe-icon-7-stroke/css/pe-icon-7-stroke.css">
<!-- Revolution style Sheets  -->
<link rel="stylesheet" href="~/evonyee/js/revolution/css/settings.css">
<link rel="stylesheet" href="~/evonyee/js/revolution/css/layers.css">
<link rel="stylesheet" href="~/evonyee/js/revolution/css/navigation.css">
<!-- owl stylesheets -->
<link rel="stylesheet" href="~/evonyee/css/owl.carousel.min.css">
<link rel="stylesheet" href="~/evonyee/css/owl.theme.default.css">
<!--[if lt IE 9]>
<script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
<script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
<![endif]-->
